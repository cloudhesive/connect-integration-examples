const success = {
    MESSAGE_SENT: {
        statusCode: 200,
        body: JSON.stringify({
            message: 'OK',
            details: 'Message sent successfully'
        })
    },
};

export { success };
